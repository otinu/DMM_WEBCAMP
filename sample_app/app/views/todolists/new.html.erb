<h1>新規投稿</h1>
<!-- form_with部分 -->
<%= form_with model: @list, url:'/todolists', local:true do |f| %> <!-- このビューファイルはtodolistsコントローラと紐づけ -->
  <h4>タイトル</h4>
  <%= f.text_field :title %>  <!-- この部分はフォーム入力のための枠組みとして表示 -->
  <h4>本文</h4>
  <%= f.text_area :body %>    <!-- この部分はフ0ォーム入力のための枠組みとして表示 -->
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <%= f.submit '投稿' %>
<% end %>
